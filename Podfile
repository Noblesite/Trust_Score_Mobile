
platform :ios, '15.0'
use_frameworks!

# Optional: include public and private sources
source 'https://github.com/CocoaPods/Specs.git'
# source 'https://artifactory.air-watch.com/artifactory/api/pods/aw-sdk-cocoapods' # Uncomment when you have access

target 'Trust Score Mobile' do
  pod 'IntuneMAM'

  # Uncomment this when WS1 SDK access is granted
  # pod 'AWSDK'
end
